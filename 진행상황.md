# 진행상황 (재빈 영상 자동화 커스터마이징)

> 이 파일만 보면 "어디까지 했는지" 알 수 있게 유지한다.
> **중요한 변경을 할 때마다 아래 "변경 로그" 맨 위에 `YYYY-MM-DD 한 줄` 로 추가한다.**

---

## 변경 로그 (최신이 위)

- 2026-07-01 진행상황.md 최초 작성. 두 저장소 실제 상태 점검 완료.
- 2026-07-01 docs/knowledge/skills/ 구조 신설(_index.md + 인서트편집.md). CLAUDE.local.md에 "스킬은 _index.md 먼저" 한 줄 추가. (※ skills/는 video-agent-knowledge에 아직 미커밋)
- 2026-07-01 yt-dlp(2026.6.9) ~/.local 설치 → reference video 다운로드 가능(모듈 import·실다운로드 검증 완료).
- 2026-07-01 감사 수정 커밋 9852967을 fork(conbilab/OpenMontage)에 백업 push. 원본 PR은 생략(혼자 사용).

---

## 저장소 구조 (핵심)

- **`~/OpenMontage`** = 스톡 OpenMontage. remote: `origin`=calesthio(원본, 읽기용), `fork`=conbilab(내 백업). SSH 인증 계정 `conbilab`.
- **`docs/knowledge` 는 심볼릭 링크** → `~/video-agent-knowledge` (별도 git repo, remote `conbilab/video-agent-knowledge`).
  즉 지식베이스는 OpenMontage 안이 아니라 별도 repo를 링크해 얹은 것.

## 스톡 위에 얹은 것 (4가지 형태로만 존재)

1. `CLAUDE.local.md` — 재빈 지침. **gitignored** (추적 안 됨).
2. `docs/knowledge` 심볼릭 링크.
3. 커밋 `9852967` (감사 수정) — main보다 1 커밋 앞섬, fork에 백업됨.
4. `.env.save` — 키 채운 env 템플릿. **gitignore 안 됨 → 커밋 주의.**

> OpenMontage 원본 tracked 파일 자체를 고친 것은 **없음** (`git diff --stat` 비어 있음).

## docs/knowledge 구성

- `templates/` 12개 (포맷결정_지침, realistic-image-prompt-guideline, video-model-selection-guideline, 한국어_TTS_설정, 스타일_세차고수/제품광고/교육정보숏폼/UGC제품후기, storyboard_generator, _템플릿_뼈대, OpenMontage_한국어영상_프롬프트, 마케팅_포맷결정_딥리서치)
- `소재/` 4개 (3pH 세차: 정리·대본 v1·제작 프롬프트 v1/v2)
- `skills/` 2개 (_index.md, 인서트편집.md) — **신규, video-agent-knowledge에 미커밋**
- 최상위 md 5개 (CapCutAgent_x_OpenMontage_통합설계, 영상편집자동화_레퍼런스_수집, 영상자동화_빌드_타임라인, 홈서버_이전_원격세팅_계획, OpenMontage_업그레이드_지침)

## git 상태 스냅샷 (2026-07-01)

- OpenMontage: 브랜치 `fix/audit-safe-corrections`, untracked=`.env.save` 뿐.
- video-agent-knowledge: 브랜치 `main`, 커밋 2개, untracked=`skills/`.

---

## 지금까지 한 것

- OpenMontage 위에 재빈 지식베이스(video-agent-knowledge)를 심볼릭 링크로 얹음.
- 재빈 지침(CLAUDE.local.md) 구성.
- 파이프라인/의존성/문서 감사 수정 → 커밋 + fork 백업.
- yt-dlp 설치로 reference video 다운로드 활성화.
- 스킬 체계(_index.md) + 첫 스킬(인서트편집) 작성.

## 아직 안 끝난 것 / 다음 후보

- [ ] `docs/knowledge/skills/` 를 video-agent-knowledge에 **커밋·push** (현재 미커밋).
- [ ] `.env.save` 처리 정리 (gitignore에 넣거나 이름 정리 — 키 커밋 방지).
- [ ] 홈서버 개선 후보(우선순위순): 4→1→2→3
  - [x] 4. yt-dlp 설치(완료)
  - [ ] 1. provider_menu가 degraded 도구를 정확히 표기
  - [ ] 2. setup_offers가 무거운 ML 설치를 "1분 설정"처럼 말하지 않게
  - [ ] 3. corpus_builder 상태 판정을 실제 실행 가능성 기준으로
- [ ] 감사에서 확인된 pre-existing 실패 2건(character-animation governance, test_registry_catalog_views)은 별도 브랜치로.
